# Base ContextHub API endpoint (optional override)
# CTX_API_BASE_URL=https://api.ctxhub.net/api

# Required API key used when talking to the ContextHub tenant endpoints.
CTX_API_KEY=replace-with-your-api-key

# Optional explicit tenant identifier. If omitted, the API token must encode the tenant.
# CTX_TENANT_ID=68f9d89798a9d6c723023d63

# Optional menu slug to hydrate the primary navigation.
# THEME_MENU_SLUG=main-menu

# Optional menu ID (overrides slug) to fetch a specific menu via the authenticated API.
# THEME_MENU_ID=68fa1c4c1bb6281fc7261ed9

# Optional logo display layout: "fullwidth" (default) or "inline".
# THEME_LOGO_LAYOUT=fullwidth
CTX_API_CONTACT_FORM_ID=68fa9b3fe70d3315aedd0855